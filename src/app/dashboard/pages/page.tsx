import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function PagesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Pages Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Edit the content of your website's pages here.</p>
      </CardContent>
    </Card>
  );
}
